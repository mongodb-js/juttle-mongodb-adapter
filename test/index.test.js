var juttleMongodbAdapter = require('../');
var assert = require('assert');

describe('juttle-mongodb-adapter', function() {
  it('should work', function() {
    assert(juttleMongodbAdapter);
  });
});
